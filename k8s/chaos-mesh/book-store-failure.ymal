apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: book-store-failure
spec:
  action: pod-failure
  mode: one
  duration: "300s"
  selector:
    labelSelectors:
      app: book-store