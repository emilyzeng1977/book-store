apiVersion: chaos-mesh.org/v1alpha1
kind: NetworkChaos
metadata:
  name: network-hello-delay
spec:
  action: delay
  mode: one
  selector:
    labelSelectors:
      app: hello
  delay:
    latency: "3s"
    correlation: "100"
    jitter: "90ms"
  duration: "180s"