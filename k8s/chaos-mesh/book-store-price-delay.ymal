apiVersion: chaos-mesh.org/v1alpha1
kind: NetworkChaos
metadata:
  name: network-book-store-price-delay
spec:
  action: delay
  mode: one
  selector:
    labelSelectors:
      app: book-store-price
  delay:
    latency: "5s"
    correlation: "50"
    jitter: "90ms"
  duration: "120s"