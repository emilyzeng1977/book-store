apiVersion: chaos-mesh.org/v1alpha1
kind: NetworkChaos
metadata:
  name: network-bookstore-delay
spec:
  action: delay
  mode: one
  selector:
    labelSelectors:
      app: mongo
  delay:
    latency: "3s"
    correlation: "100"
    jitter: "90ms"
  duration: "120s"