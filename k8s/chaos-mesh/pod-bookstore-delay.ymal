apiVersion: chaos-mesh.org/v1alpha1
kind: NetworkChaos
metadata:
  name: network-bookstore-delay
spec:
  action: delay
  mode: one
  selector:
    labelSelectors:
      app: bookstore
  delay:
    latency: "6s"
    correlation: "25"
    jitter: "90ms"
  duration: "120s"